# Base de datos
DATABASE_URL="postgresql://usuario:contraseña@localhost:5432/ecommerce"

# Secreto para firmar sesiones. Genéralo con: openssl rand -base64 32
AUTH_SECRET=""

# Correo saliente (opcional).
# Sin estas variables, los correos se escriben en la consola del servidor
# en vez de enviarse.
SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASSWORD=""
MAIL_FROM=""
